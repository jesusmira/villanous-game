# Check de skills

1. Abre un componente grande de UI.
2. Pega este prompt:

   "Usa las skills `clean-architecture-ui` y `mcp-eslint-wrapper` para:
   - Detectar si este componente es demasiado grande.
   - Proponer una versión refactorizada con subcomponentes y hooks.
   - Señalar qué reglas de lint se estarían violando (max-lines, complexity, etc.)."

3. Verifica que Claude:
   - Menciona explícitamente las skills.
   - Propone refactors concretos.
   - Habla de tamaño, complejidad y arquitectura.
